include "application.conf"

# Any properties that differ between application.conf and production.conf are left in place below.

metrics.slf4j = true
health.logging = true

# Maximum number of messages in RabbitMQ queue
rabbit.messages.max=1000
